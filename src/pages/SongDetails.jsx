const SongDetails = () => (
  <div className="text-center text-2xl font-semibold text-white">
    Song Detail Page :This page could not be build because API is not responding
    well
  </div>
);

export default SongDetails;
